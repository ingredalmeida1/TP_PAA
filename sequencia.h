#include <stdlib.h>
#include <stdio.h>
#include <string.h>

void inicializaVetorSequencia(int **vetorSequencia, int N, int M);

int sequenciaFibonacci(int N);

void preencheVetorFib(int **vetorFib, int N, int M);

void preencheVetorSequencia(int **vetorFib, int **vetorSequenci, int N, int M);

void imprimeSequencia(int N, int M, int **vetorSequencia);